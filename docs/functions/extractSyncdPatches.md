[**@innovatorssoft/baileys v7.3.5**](../README.md)

***

[@innovatorssoft/baileys](../globals.md) / extractSyncdPatches

# Function: extractSyncdPatches()

> **extractSyncdPatches**(`result`, `options`): `Promise`\<\{ `critical_block`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `critical_unblock_low`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `regular`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `regular_high`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `regular_low`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; \}\>

Defined in: [lib/Utils/chat-utils.d.ts:38](https://github.com/innovatorssoft/Baileys/blob/04838aff6e2fc8f9aa3ebaf00c4a148759d7bbd4/lib/Utils/chat-utils.d.ts#L38)

## Parameters

### result

[`BinaryNode`](../type-aliases/BinaryNode.md)

### options

`AxiosRequestConfig`\<\{\}\>

## Returns

`Promise`\<\{ `critical_block`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `critical_unblock_low`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `regular`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `regular_high`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; `regular_low`: \{ `hasMorePatches`: `boolean`; `patches`: `ISyncdPatch`[]; `snapshot`: `any`; \}; \}\>
